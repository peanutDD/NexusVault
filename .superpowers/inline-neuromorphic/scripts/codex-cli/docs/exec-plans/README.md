## 执行计划（exec-plans）

这里放对 `codex-cli` 的任务执行计划样例/模板，目标是让改动“可追溯、可复盘、可回滚”。

### 推荐模板

每次对核心行为做修改（CLI 参数、输出 JSON 契约、Patch 应用策略、默认过滤策略）时，建议写一个简短计划，包含：

- 目标与非目标
- 假设与风险
- 依赖（外部工具/环境变量）
- 验收标准（至少包含 fmt/clippy/test）

### 说明

该目录用于“计划归档”，不会被运行时读取。

